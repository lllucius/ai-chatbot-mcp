"""
Shared package containing schemas and models used by both API and SDK.

This package provides common data models and schemas that can be imported
by both the main API application and the client SDK, eliminating duplication
and ensuring consistency between API definitions and client implementations.
"""
